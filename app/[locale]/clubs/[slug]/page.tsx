// Listing profiles serve at their canonical type-prefixed URL
// (e.g. /clubs/sunday-run-club) — the shared page lives in listing/[slug].
export { default, generateMetadata } from "@/app/[locale]/listing/[slug]/page";
